<?xml version="1.0" encoding="utf-8"?>
<paths  xmlns:android="http://schemas.android.com/apk/res/android">
    <files-path name="files_path" path="." /> <!-- Context.getFilesDir(). -->
    <cache-path name="cache_path" path="." /> <!-- getCacheDir(). -->
    <external-path name="external_files" path="." /> <!-- Environment.getExternalStorageDirectory(). -->
    <external-files-path name="external_files_path" path="." /> <!-- Context#getExternalFilesDir(String) Context.getExternalFilesDir(null). -->
    <external-cache-path name="external_cache_path" path="." /> <!-- Context.getExternalCacheDir(). -->
</paths>
